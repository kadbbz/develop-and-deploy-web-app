{
  "name": "develop-and-deploy-web-app",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "scripts": {
    "test": "npm run test:fast && npm run test:full",
    "test:fast": "node --test --test-concurrency=1 tests/fast/*.test.js",
    "test:full": "node --test --test-concurrency=1 tests/full/*.test.js"
  }
}
